/**
  * Created by boki on 5/30/16.
  */
object add{
  // return type could be any valid scala data type and list of parameters
  def addInt( a:Int, b:Int ) : Int = {
    var sum:Int = 0
    sum = a + b

    return sum
  }
}
